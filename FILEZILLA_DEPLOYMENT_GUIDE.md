# คู่มือการตั้งค่า FileZilla Pro สำหรับการ Deploy โปรเจกต์

## ภาพรวม

คู่มือนี้อธิบายวิธีการเชื่อมต่อระหว่างโปรเจกต์พัฒนาของคุณกับเซิร์ฟเวอร์ Windows ระยะไกล โดยใช้ FileZilla Pro สำหรับการ Deploy และซิงโครไนซ์ไฟล์

---

## การตั้งค่าการเชื่อมต่อ

### รายละเอียดเซิร์ฟเวอร์

| การตั้งค่า | ค่า                                              |
| ---------- | ------------------------------------------------ |
| โปรโตคอล   | SFTP หรือ FTP (ขึ้นอยู่กับการตั้งค่าเซิร์ฟเวอร์) |
| โฮสต์      | 192.168.1.10                                     |
| ชื่อผู้ใช้ | Administrator                                    |
| พอร์ต      | 22 (SFTP) หรือ 21 (FTP)                          |

---

## ขั้นตอนการตั้งค่า

### 1. สร้าง Site ใหม่ใน Site Manager

1. เปิด FileZilla Pro
2. ไปที่ **File → Site Manager** (หรือกด `Ctrl + S`)
3. คลิก **New Site**
4. ตั้งชื่อตามโปรเจกต์ของคุณ (เช่น `TPP-Production-Server`)

### 2. กำหนดค่าการเชื่อมต่อ

#### แท็บ General (ทั่วไป)

| การตั้งค่า | ค่า                                                  |
| ---------- | ---------------------------------------------------- |
| Protocol   | SFTP - SSH File Transfer Protocol (แนะนำ) หรือ FTP   |
| Host       | `192.168.1.10`                                       |
| Port       | ปล่อยว่างสำหรับค่าเริ่มต้น หรือระบุ `22` สำหรับ SFTP |
| Logon Type | Normal                                               |
| User       | `Administrator`                                      |
| Password   | ใส่รหัสผ่านเซิร์ฟเวอร์ของคุณ                         |

#### แท็บ Advanced (ขั้นสูง)

| การตั้งค่า               | ค่า                                         |
| ------------------------ | ------------------------------------------- |
| Default remote directory | `/C:/Users/Administrator/YourProjectFolder` |
| Default local directory  | `C:\Users\YourName\Projects\your-project`   |

### 3. การตั้งค่าการถ่ายโอน (ไม่บังคับ)

| การตั้งค่า                     | ค่า                                       |
| ------------------------------ | ----------------------------------------- |
| Transfer Mode                  | Passive (แนะนำสำหรับ Firewall)            |
| Limit simultaneous connections | เลือกถ้าเซิร์ฟเวอร์มีข้อจำกัดการเชื่อมต่อ |

---

## ขั้นตอนการทำงานสำหรับโปรเจกต์ Next.js/React

### การแมปโครงสร้างไดเรกทอรี

```
เครื่องพัฒนา (Local)               เซิร์ฟเวอร์ (Remote)
─────────────────────             ─────────────────────
/your-project                     /C:/inetpub/wwwroot/your-project
  ├── .next/                        ├── .next/
  ├── public/                       ├── public/
  ├── node_modules/                 ├── node_modules/
  └── package.json                  └── package.json
```

### ไฟล์ที่ต้องอัปโหลด (Production Build)

หลังจากรันคำสั่ง `npm run build`:

- โฟลเดอร์ `.next/` (ผลลัพธ์ที่คอมไพล์แล้ว)
- โฟลเดอร์ `public/` (ไฟล์ static)
- `package.json` และ `package-lock.json`
- `.env.production` (ตัวแปรสภาพแวดล้อม)
- `next.config.js`

### ไฟล์ที่ควรยกเว้น

สร้างตัวกรองใน FileZilla เพื่อยกเว้น:

- `node_modules/` (ติดตั้งบนเซิร์ฟเวอร์แทน)
- `.git/`
- `.env.local`
- `*.log`

---

## การตั้งค่า Synchronized Browsing (การเรียกดูแบบซิงโครไนซ์)

1. ใน **Site Manager → แท็บ Advanced**
2. เปิดใช้งาน **Synchronized Browsing**
3. ตั้งค่าไดเรกทอรี local และ remote ให้ตรงกัน
4. ทำให้ทั้งสองแผงซิงค์กันเมื่อนำทาง

---

## รายการตรวจสอบการ Deploy อย่างรวดเร็ว

- [ ] Build โปรเจกต์ในเครื่อง: `npm run build`
- [ ] เชื่อมต่อกับเซิร์ฟเวอร์ผ่าน FileZilla
- [ ] อัปโหลด `.next/`, `public/`, และไฟล์ config
- [ ] รันคำสั่ง `npm install --production` บนเซิร์ฟเวอร์ (ถ้าจำเป็น)
- [ ] รีสตาร์ทแอปพลิเคชัน/บริการ IIS

---

## คำแนะนำด้านความปลอดภัย

| คำแนะนำ                    | รายละเอียด                                                             |
| -------------------------- | ---------------------------------------------------------------------- |
| ใช้ SFTP                   | ใช้ SFTP แทน FTP ธรรมดาเพื่อการถ่ายโอนข้อมูลแบบเข้ารหัส                |
| การยืนยันตัวตนด้วย SSH Key | ใช้การยืนยันตัวตนด้วย SSH key แทนรหัสผ่านถ้าเป็นไปได้                  |
| ผู้ใช้เฉพาะ                | จำกัดการเข้าถึง Administrator — สร้างผู้ใช้สำหรับ deploy โดยเฉพาะ      |
| กฎ Firewall                | เปิดใช้กฎ Firewall เพื่อจำกัดการเข้าถึงเฉพาะ IP ที่เชื่อถือได้เท่านั้น |

---

## การแก้ไขปัญหาที่พบบ่อย

### การเชื่อมต่อหมดเวลา (Connection Timeout)

- ตรวจสอบว่าที่อยู่ IP ของเซิร์ฟเวอร์ถูกต้อง
- ตรวจสอบว่า Firewall ของเซิร์ฟเวอร์อนุญาตการเชื่อมต่อบนพอร์ต 22/21
- ตรวจสอบว่าบริการ SFTP/FTP กำลังทำงานอยู่บนเซิร์ฟเวอร์

### การยืนยันตัวตนล้มเหลว (Authentication Failed)

- ตรวจสอบชื่อผู้ใช้และรหัสผ่านอีกครั้ง
- ตรวจสอบว่าผู้ใช้มีสิทธิ์เข้าถึงไดเรกทอรีเป้าหมาย
- ตรวจสอบว่าต้องใช้ SSH key แทนรหัสผ่านหรือไม่

### ข้อผิดพลาดในการถ่ายโอน (Transfer Errors)

- ตรวจสอบว่ามีพื้นที่ดิสก์เพียงพอบนเซิร์ฟเวอร์
- ตรวจสอบสิทธิ์ไฟล์บนไดเรกทอรีระยะไกล
- ตรวจสอบความเสถียรของเครือข่าย

---

## แหล่งข้อมูลเพิ่มเติม

- [เอกสาร FileZilla Pro](https://filezillapro.com/docs/)
- [คู่มือการ Deploy Next.js](https://nextjs.org/docs/deployment)
- [การตั้งค่า Windows Server IIS](https://docs.microsoft.com/en-us/iis/)
